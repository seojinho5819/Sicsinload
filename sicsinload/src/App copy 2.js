import React, { useEffect, useState, useCallback, useRef } from 'react';
import Marker from '';

const NaverMap = ({ naver, setMarkerInfoList }) => {
	if (!naver) {
		return <div>Loading...</div>;
	}

	// return naverMap...
};
